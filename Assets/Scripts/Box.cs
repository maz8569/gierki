using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Box : MonoBehaviour
{
    public packageType type = packageType.BLUE;
    // Start is called before the first frame update
    
}
